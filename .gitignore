# Python
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
*.egg
dist/
build/
.eggs/

# Virtual environment
.venv/
venv/
env/

# Dataset (too large for git)
Chest_X-Ray_Images_archive/
archive/

# Trained models — Exp3 ResNet50 files are too large for GitHub (>100 MB each)
# Exp1 and Exp2 models + all JSON metrics files ARE tracked (small enough)
results_xray/exp3_xray_resnet_scratch.keras
results_xray/exp3_xray_resnet_scratch_FIXED.keras
results_xray/exp3_xray_resnet_transfer.keras

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*.suo
*.sw?

# Environment variables (never commit secrets)
.env
.env.*
!.env.example

# Logs
*.log
